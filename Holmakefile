ifndef VMLDIR
VMLDIR = ../vml
endif

HOLHOL = $(VMLDIR)/hol-light
INCLUDES = $(HOLHOL)/syntax-lib $(HOLHOL)/set-theory $(HOLHOL)/stateful/syntax $(HOLHOL)/stateful/semantics
OPTIONS = QUIT_ON_FAILURE

ifdef POLY
HOLHEAP = heap
PARENT_HOLHEAP = $(HOLHOL)/stateful/semantics/heap
EXTRA_CLEANS = $(HOLHEAP) $(HOLHEAP).o

BARE_THYS = $(HOLHOL)/syntax-lib/holSyntaxLibTheory $(HOLHOL)/set-theory/setSpecTheory $(HOLHOL)/stateful/syntax/holAxiomsSyntaxTheory

DEPS = $(patsubst %,%.uo,$(BARE_THYS))

$(HOLHEAP): $(DEPS)
	$(protect $(HOLDIR)/bin/buildheap) -b $(PARENT_HOLHEAP) -o $(HOLHEAP) $(BARE_THYS)
endif
